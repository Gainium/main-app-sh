# 095 — plan

## Build order

1. Red test `src/bot/dca/canceledTpRestore.harness.spec.ts` — the real
   `processCanceledOrder` schedules nothing for an unfilled external cancel
   (§1.2), `restoreCanceledTp` / `isOwnCancel` do not exist.
2. Pure rules in `src/bot/dca/canceledTpRestore.ts`:
   `isUnattributedUnfilledTpCancel` (§4.1, §4.2) and `decideCanceledTpRestore`
   (§4.4, §4.5).
3. `main.ts`: `ownCancels` + `noteOwnCancel` / `isOwnCancel`; record in
   `cancelOrderOnExchange` before the venue call and in `primeCancelBatch`
   before the bulk call (§4.1).
4. `dcaHelper.ts`: `processCanceledOrder`'s zero-fill branch arms
   `armCanceledTpRestore`; `restoreCanceledTp` under `@IdMute` on the
   `placeOrders` key places the plan's take-profits through
   `placeOrdersHoldingDealLock` with fresh ids (§4.3, §4.6); timers cleared
   in `clearClassProperties` (§4.7).

## Legacy tier

Tier 1: the decisions are extracted as pure functions next to the other
`dca/` rules (`partialBaseEntry`, `multiTpCompletion`); the engine keeps only
the I/O — reading its own state, the timer, the placement call.

## Blast Radius

1. Call sites: `processCanceledOrder` is reached only from the order queue's
   `onCanceled`; combo overrides it (§4.8). `cancelOrderOnExchange` gains one
   write before its venue call and is otherwise unchanged; `primeCancelBatch`
   (Kraken spot only) likewise. `placeOrdersHoldingDealLock` is called, not
   changed — its close-in-flight guard still applies (no
   `afterTerminalCloseRefusal`).
2. Under `core/` (main-app-sh): ships to every consumer on its next core bump.
   Runs inside the bot workers — which restart on an `app` deploy.
3. Contracts: none. No schema, GraphQL, socket, queue or DB field changes;
   the record is in memory only.
4. Tests on this path that must stay green: `venueCanceledBaseEntry`,
   `comboGridCancelLedger`, every `cancelOrderOnExchange` / `primeCancelBatch`
   harness (they build `MainBot` off the prototype — hence the lazy map).

## Traceability

| Spec | Test |
|---|---|
| §1.1, §4.6 | engine — re-places the planned TP with a fresh id |
| §4.1 | rules + engine — own cancel schedules nothing; MainBot records before the venue call |
| §4.2 | rules + engine — a cancel carrying fills keeps the partial-TP path only |
| §4.4 | rules (every skip) + engine — resting TP, closing, market close |
| §4.5 | rules + engine — defers under `blockCheck`, gives up after the budget |
| §4.6 | engine — skips filled targets, multi-TP prefix |
| §4.7, §4.8 | code review (timer map in `clearClassProperties`; combo override untouched) |
