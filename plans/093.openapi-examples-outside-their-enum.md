# 093 — plan

## Solution

1. `src/utils/openapiEnumExamples.ts` — pure `findEnumExampleMismatches(node)`
   (Tier 2, no I/O).
2. `generate-openapi-schemas.ts` — `main()` runs it over
   `components.schemas` before writing and throws (the YAML is not written).
   Covers generated and hand-written schemas alike.
3. Per-schema `schemaFieldMetadata` override (`SettingsIndicators.type` →
   `RSI`); `indicatorInterval` → `1h`; `stopStatus` → `closed`.
4. Hand-edit `DCABotExtended.previousStatus` example → `open`; regenerate.

## Blast Radius

- `propertyToOpenAPI` gains a `typeName` arg; both callers are in the same
  class. `fieldMetadata` for other schemas is unchanged (regenerated diff is
  the four example lines only).
- File is under `core/` (main-app-sh, public). Published document changes
  `example:` text only — no enum, type or validator change.
- `openapiGridEnumFields.spec.ts` (spec 092) must stay green.

## Traceability

| spec | test |
|---|---|
| §1.1 / §1.2 | `src/server/v2/openapiEnumExamples.spec.ts` §1 |
| §3 checker | `src/server/v2/openapiEnumExamples.spec.ts` §3 |
