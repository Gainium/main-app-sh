# 094 — plan

## Build order

1. Red test `src/server/v2/openapiInlineUnionEnums.spec.ts` against the
   committed `openapi-v2.yaml` (§4.4).
2. `generate-openapi-schemas.ts`: hoist the `literalText` helper out of
   `parseStringLiteralUnion` into a private method shared by it and
   `parseProperty`; in `parseProperty`'s union branch, when every non-null
   member is a string literal, emit `{ type: 'string', enum: [all texts] }`.
3. `npm run generate:schemas`, diff the YAML: every hunk must be an enum gaining
   members its TS union already declares (§4.1) — nothing else.

## Legacy tier

Tier 0 (in-place): a local branch in an existing generator method plus reuse of
an existing helper. No extraction warranted — the generator is a dev-time
script with no I/O besides reading `types.ts` and writing the YAML.

## Blast Radius

1. Call sites: `parseProperty` is the only property parser; `literalText` had one
   caller (`parseStringLiteralUnion`) and keeps it.
2. Under `core/` (main-app-sh, public). Only the generator script and the
   generated YAML change; no runtime code path, no validator, no types.
3. Contract: the public `/api/v2` OpenAPI document (Danger List #2) — the
   DOCUMENT widens enums to match what the API already accepts; the API itself
   is unchanged. gainium-mcp / n8n read the served spec for docs only.
4. Other tests on this YAML: `openapiGridEnumFields.spec.ts` (092),
   `openapiEnumExamples.spec.ts` (093) must stay green.

## Traceability

| Spec | Test |
|---|---|
| §1.1, §4.4 | `openapiInlineUnionEnums.spec.ts` — stopStatus enum ⊇ closed, monitoring |
| §4.4 | same — every stopStatus example is inside its enum |
| §4.1, §4.3 | YAML diff review (step 3) + 092/093 specs stay green |
