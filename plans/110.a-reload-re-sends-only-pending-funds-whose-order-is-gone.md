# Plan 110

## Solution

- `src/bot/dca/pendingFundsResend.ts` (new, pure): `splitPendingFunds(pending,
  orders, key)` splits the entries into `standing` (an order carrying the id is
  NEW / PARTIALLY_FILLED / FILLED) and `resend`.
- `dcaHelper.resendPendingFunds(d)` replaces the four copy-pasted blocks in
  `restoreWork` and `updateDealSettings`. It keeps `standing` in the deal's list
  and re-sends only `resend`, through the same `addDealFunds` /
  `reduceDealFunds` calls as before.
- JIT tier 2 for the selection, since the decision is pure. The orchestration
  stays in the helper.

## Blast radius

1. Call sites: only the two re-send sites. `comboHelper` has no pending-funds
   re-send (`addDealFunds` returns early for combo).
2. `core/` file: shared by the app and self-hosted builds. No type, schema,
   GraphQL or event change.
3. No contract changes. `pendingAddFunds` / `pendingReduceFunds` keep their
   shape, with fewer entries cleared.
4. Other tests: `ladderLevels.spec.ts`, `addFundsLadderSlot.harness.spec.ts`,
   `reduceFunds*.spec.ts`, and the app's `tests/botSettingsKeepOrders.ts`.

## Traceability

| Spec | Test |
|---|---|
| §1.1.1 | harness §1.1, §1.3, unit §2.1 |
| §1.1.2 | harness §1.2, unit §2.2 |
| §1.1.3 | harness §1.4, unit §2.3 |
| §1.1.4 | unit §2.1 (FILLED) |
| §1.1.5 | app `tests/botSettingsKeepOrders.ts` (unchanged, green) |
